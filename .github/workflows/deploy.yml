name: CI/CD zu Plesk

# 1. Trigger: Wann soll die Pipeline starten?
on:
  push:
    branches: [ "main" ] # Startet, sobald du Code in den main-Branch pushst

jobs:
  # JOB 1: Testen
  test-code:
    runs-on: ubuntu-latest
    steps:
      - name: Code auschecken
        uses: actions/checkout@v4

      - name: PHP Setup
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3.30' # Passe dies an deine Plesk-PHP-Version an

      - name: PHP Syntax Check (Lint)
        run: |
          # Prüft alle PHP-Dateien auf Syntaxfehler (z.B. fehlende Semikolons)
          find . -name "*.php" -print0 | xargs -0 -n1 php -l

 # JOB 2: Deployment
  deploy-to-plesk:
    needs: test-code
    runs-on: ubuntu-latest
    steps:
      - name: Code auschecken
        uses: actions/checkout@v4

      - name: Dateien via FTP hochladen
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: github-deploy
          password: ${{ secrets.FTP_PASSWORD }}
          # WICHTIG: Da github-deploy schon im richtigen Ordner startet:
          server-dir: ./ 
          protocol: ftps
          port: 21
          timeout: 60000
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            node_modules/**
            .env